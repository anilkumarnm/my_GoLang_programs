package main // Use of word main would mean it is an executable package.

import "fmt" //Used to give our package access to some code written in another package. Gives access to fmt library

func main() { //func is nothing but short for function. You can pass arguments in the () brackets
	fmt.Println("Hi There")
}
